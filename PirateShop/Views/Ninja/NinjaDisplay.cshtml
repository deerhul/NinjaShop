@model PirateShop.Models.ViewModels.NinjaViewModel

@{
    ViewBag.Title = "View";
}

<h2>View</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table table-hover">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Age)
        </th>
        <th>@Html.DisplayNameFor(model => model.Clan.ClanName)</th>
        <th></th>
    </tr>

    @*<div class="container">
            <div class="row">
                <div class="col-sm-">
                    @Html.DisplayNameFor(model => model.Name)
                </div>
                <div class="col-sm-">
                    @Html.DisplayNameFor(model => model.Age)
                </div>
                <div class="col-sm-">
                    @Html.DisplayNameFor(model => model.clan)
                </div>
            </div>
        </div>*@

    @*@foreach (var item in Model) {
            <div class="container">
                <div class="row">
                    <div class="col-sm">
                        @Html.DisplayFor(modelItem => item.Name)
                    </div>
                    <div class="col-sm">
                        @Html.DisplayFor(modelItem => item.Age)
                    </div>
                    <div class="col-sm">
                        @Html.DisplayFor(modelItem => item.clan)
                    </div>
                </div>
            </div>
        }*@


    @foreach (var item in Model.Ninjas)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Age)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.clan.ClanName)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.ID }) |
                @Html.ActionLink("Details", "Details", new { id = item.ID }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.ID })
            </td>
        </tr>
    }

</table>
